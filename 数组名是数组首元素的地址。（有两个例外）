#include<stdio.h>
/*
数组名是数组首元素的地址。（有两个例外）
1. sizeof(数组名)，计算整个数组的大小，sizeof内部单独放一个数组名，数组名表示整个数
组。
2. &数组名，取出的是数组的地址。&数组名，数组名表示整个数组。
除此1,2两种情况之外，所有的数组名都表示数组首元素的地址。
除此1,2两种情况之外，所有的数组名都表示数组首元素的地址。
*/
int main()//通过输出可以看出各个差异
{
	int a[] = { 1,2,3,4,5,6,7 };//每个元素占4个字节，共有7个元素
	printf("%p\n", a); //数组名是数组首元素的地址。
	printf("%p\n\n", a+1);

	printf("%p\n", &a[0]);//数组首元素的地址。printf("%p\n", &a[0]);<=>printf("%p\n", a);
	printf("%p\n\n", &a[0]+1);

	printf("%p\n", &a);// &数组名，取出的是数组的地址。&数组名，数组名表示整个数组。
	printf("%p\n\n", &a+1);
	return 0;
}
