#include<stdio.h>
int MAX(int x, int y)
{
	if (x > y)
		return x;
	else
		return y;
}
int main()
{
	int a = 6;
	int b = 4;
	int max = 0;
	printf("请输入两个数：\n");
	scanf_s("%d%d", &a, &b);
	max = MAX(a, b);
	printf("最大值:%d",max);
	return 0;
}
